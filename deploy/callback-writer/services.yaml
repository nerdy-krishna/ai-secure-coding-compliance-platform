apiVersion: v1
kind: Service
metadata:
  name: c11-private-writer
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: c11-private-writer}
  ports:
    - {name: writer-mtls, port: 9443, targetPort: writer-mtls, protocol: TCP}
