# Pinned latest-stable versions for the SCCAP docs build.
# Local preview:
#     cd docs
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements-docs.txt
#     mkdocs serve
mkdocs>=1.6,<2.0
mkdocs-material>=9.5,<10.0
pymdown-extensions>=10.7,<11.0
