# Inactive pre-baseline revisions

This directory is retained for audit and controlled upgrades from revisions
older than `4d5e6f708192`. It is not part of Alembic's active
`version_locations`; new revisions belong in `alembic/current_versions`.
